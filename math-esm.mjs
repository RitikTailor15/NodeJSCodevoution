// ******** First Pattern ********
// const add = (a, b) => {
//   return a + b;
// };

// export default add;

// ******** Second Pattern ********
// export default (a, b) => {
//   return a + b;
// };

// ******** Third Pattern ********
// const add = (a, b) => {
//   return a + b;
// };

// const subtract = (a, b) => {
//   return a - b;
// };

// export default { add, subtract };

// ******** Fourth Pattern ********
export const add = (a, b) => {
  return a + b;
};

export const subtract = (a, b) => {
  return a - b;
};
